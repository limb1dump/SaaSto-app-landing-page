import './Navbar.scss'
import { images } from '../../constants';

const Navbar = () => {
    const menu = ['Demos', 'Features', 'Pricing', 'Contact']

    return (
        <nav>
        </nav>
    );
};

export default Navbar;